import React from "react";
import {Tilt} from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]'
      >
        Hey, my name is Rehman Mohammed. I'm a software engineer from Toronto, Ontario, currently studying Computer Engineering at McMaster University. Proficient in Python, Java, SQL, and JavaScript, I've built full-stack applications using frameworks like ReactJS and Node.js, developed robust backend systems, and created interactive data dashboards using industry tools like Tableau, Power BI, and Looker. I have experience constructing ETL pipelines using tools like Apache Airflow, Apache Spark, and Python libraries. Additionally, I'm skilled in using cloud platforms such as AWS and Google Cloud Platform to deploy and manage applications. Outside of my professional interests, I enjoy participating in marathons throughout Toronto to stay fit and connected with my community. Thank you for visiting my website. Feel free to explore my projects and experiences, and don’t hesitate to reach out if you'd like to connect or collaborate!


      </motion.p>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
